import { Typography } from '@mui/material';
import MainCard from 'components/MainCard';


const AboutPage = () => {
  return (
    <MainCard title="About">
      <Typography variant="body2">
        It is about studyeasy and React.
      </Typography>
    </MainCard>
  );
};

export default AboutPage;
